Review current changes.

1. Summarize changed files with `git diff --stat`.
2. If frontend changed, run `cd frontend; npm run lint`.
3. If backend changed, run `cd backend; python -m pytest tests -v`.
4. List likely regressions and missing tests.
5. Suggest next steps.
